import Home from '../../src/index';

// Home Component Tests

beforeEach(() => {
    // mount Home
});
afterEach(() => {
    // unmount Home
})

describe('handleAccountSelect tests', () => {
})
describe('handleGetData tests', () => {
})
describe('handleClearData tests', () => {
})
describe('handleAccountRemove tests', () => {
})
describe('handleAccountSelect test', () => {
})